## 实施计划

修改 `videos.ts`，让它使用已有的 JSON 配置文件：

### 现有 JSON 文件
- `danmaku.json` - 弹幕数据（bloodDanmaku/mixDanmaku/commonDanmaku）
- `titles.json` - 称号数据（按 blood/mix 主题分类）
- `voices.json` - C言C语数据（按 blood/mix 主题分类）

### 需要处理
1. **创建 `canteenCategories.json`** - 食堂分类数据（目前没有对应 JSON）
2. **修改 `videos.ts`** - 从各 JSON 文件导入数据，移除重复定义

### 变更文件
1. 新建：`frontend/src/features/yuxiaoc/data/canteenCategories.json`
2. 修改：`frontend/src/features/yuxiaoc/data/videos.ts`