# 直播间链接按钮位置调整 - TDD实施方案

## 问题分析

当前导航栏结构：
1. **左侧**：Logo (皇冠图标) + "C皇驾到"文字标题
2. **中间**：导航链接（首页、称号、食堂、语录）
3. **右侧**：外部链接（斗鱼直播间、B站合集、鱼吧链接）+ 主题切换按钮

用户需求：将"斗鱼直播间"链接按钮移动到"C皇驾到"文字的位置。

## TDD实施计划

### 步骤1：编写测试用例

**新测试用例**：

1. **TC-013: 直播间链接按钮位置测试**
   - 测试目标：验证"斗鱼直播间"链接按钮显示在Logo旁边
   - 预期结果：
     - 页面中存在"斗鱼直播间"链接按钮
     - 链接按钮位于Logo（皇冠图标）旁边
     - 链接指向正确的直播间URL

2. **TC-014: 重复直播间链接移除测试**
   - 测试目标：验证右侧外部链接区域不再显示斗鱼直播间链接
   - 预期结果：右侧外部链接区域只显示"B站合集"和"鱼吧链接"

3. **TC-015: Logo首页跳转功能测试**
   - 测试目标：验证皇冠Logo仍然作为首页跳转按钮
   - 预期结果：Logo点击后能正确跳转到hero区域

### 步骤2：运行现有测试（验证初始状态）

```bash
# 运行Header组件现有测试
npm test -- tests/unit/components/features/yuxiaoc/Header.test.tsx

# 验证测试通过，记录初始状态
```

### 步骤3：修改Header组件实现

**修改点**：

1. **Header.tsx** - Logo和标题区域（第114-141行）：
   - 将 "C皇驾到" h1 元素替换为 "斗鱼直播间" 链接按钮
   - 保留皇冠Logo图标，作为首页跳转按钮
   - 设计新的直播间链接按钮样式

2. **Header.tsx** - 外部链接区域（第190-232行）：
   - 过滤掉"live"（斗鱼直播间）链接，避免重复
   - 只保留"B站合集"和"鱼吧链接"

### 步骤4：运行新测试（验证修改结果）

```bash
# 运行更新后的测试
npm test -- tests/unit/components/features/yuxiaoc/Header.test.tsx

# 验证所有测试通过，包括新添加的测试用例
```

### 步骤5：运行完整测试套件（确保无回归）

```bash
# 运行整个yuxiaoc模块的测试
npm test -- tests/unit/components/features/yuxiaoc/

# 验证所有测试通过，确保修改没有破坏其他功能
```

### 步骤6：验证覆盖率

```bash
# 运行测试覆盖率报告
npm run test:coverage -- tests/unit/components/features/yuxiaoc/Header.test.tsx

# 确保覆盖率达到80%以上
```

## 预期修改效果

**修改后导航栏结构**：
1. **左侧**：Logo (皇冠图标，首页跳转) + "斗鱼直播间"链接按钮
2. **中间**：导航链接（首页、称号、食堂、语录）
3. **右侧**：外部链接（B站合集、鱼吧链接）+ 主题切换按钮

## 技术实现要点

1. **样式设计**：确保新的直播间链接按钮与整体导航栏风格一致
2. **响应式设计**：确保在不同屏幕尺寸下正常显示
3. **功能完整性**：保留所有现有功能，只调整布局
4. **测试覆盖率**：确保所有修改都有相应的测试覆盖

## 风险控制

- **回归测试**：确保所有现有测试仍然通过
- **视觉一致性**：保持导航栏的整体美观度
- **功能完整性**：确保所有链接和按钮功能正常
- **测试覆盖**：确保新功能有充分的测试覆盖