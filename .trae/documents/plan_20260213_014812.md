## 实施计划

将 yuxiaoc 页面的视频数据改为通过 JSON 配置文件配置：

### 步骤 1：创建 `videos.json`
- 将 `videos.ts` 中的 `videos` 数组提取到新的 `videos.json` 文件

### 步骤 2：修改 `videos.ts`
- 参考 lvjiang 的模式，从 `videos.json` 导入视频数据
- 保留其他数据（danmakuList、titles、cVoices、canteenCategories）在 ts 文件中（因为它们不是视频数据）

### 变更文件：
1. 新建：`frontend/src/features/yuxiaoc/data/videos.json`
2. 修改：`frontend/src/features/yuxiaoc/data/videos.ts`